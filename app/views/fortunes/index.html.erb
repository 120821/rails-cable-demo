<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<canvas id="myChart2" width="400" height="400"></canvas>
<script>
const ctx = document.getElementById('myChart2').getContext('2d');
const myChart = new Chart(ctx, {
    type: 'bar',
    data: {
        labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
        datasets: [{
            label: '# of Votes',
            data: [12, 19, 3, 5, 2, 3],
            backgroundColor: [
                'rgba(255, 99, 132, 0.2)',
                'rgba(54, 162, 235, 0.2)',
                'rgba(255, 206, 86, 0.2)',
                'rgba(75, 192, 192, 0.2)',
                'rgba(153, 102, 255, 0.2)',
                'rgba(255, 159, 64, 0.2)'
            ],
            borderColor: [
                'rgba(255, 99, 132, 1)',
                'rgba(54, 162, 235, 1)',
                'rgba(255, 206, 86, 1)',
                'rgba(75, 192, 192, 1)',
                'rgba(153, 102, 255, 1)',
                'rgba(255, 159, 64, 1)'
            ],
            borderWidth: 1
        }]
    },
    options: {
        scales: {
            y: {
                beginAtZero: true
            }
        }
    }
});
</script>

<h3>china</h3>
<p>总数: <%= @total_count %></p>
<table class='table table-striped table-hover table-bordered'>
  <tr>
    <th>industry:</th>
    <th>mount:</th>
    <th>创建时间:</th>
    <th>操作:</th>
  </tr>
  <% @mounts.each do |fortune| %>
    <tr>
      <td><%= fortune.industry %></td>
      <td><%= fortune.industry.size %></td>
      <td><%= fortune.created_at.to_s(:db) %></td>
      <% if session[:current_user] == "admin" %>
        <%= link_to raw("编辑&gt;&gt;"), (edit_fortune_path(:id => china_fortune.id) if session[:current_user] == "admin"), class: 'detail1' %>
        <%= link_to "删除",fortune,:method => :delete if session[:current_user] == "admin"%>
      <% end %>
    </tr>
  <% end %>
</table>

2020 500中国
  <% @mounts.each do |fortune| %>
   '<%= fortune.industry %>',
  <% end %>
  ===================================
  <% @mounts.each do |fortune| %>
    <%= fortune.industry.size %>,
  <% end %>
  ===================================
  hijihihih
